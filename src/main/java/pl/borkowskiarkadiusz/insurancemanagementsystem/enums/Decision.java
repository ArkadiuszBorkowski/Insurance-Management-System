package pl.borkowskiarkadiusz.insurancemanagementsystem.enums;

/**
 * Enum representing the decision status of a claim in the system.
 * This enum includes different possible decisions for a claim.
 *
 * ANALIZA is the default status for a new claim.
 */
public enum Decision {
    ANALIZA,
    AKCEPTACJA,
    ODMOWA
}
